# dsh plugin 安装后将本包加入 profile 的 bundle 层，自动插入复盘插件。
# 必须使用裸包名，客户端模块发现器才能读取 package.json 中的 dsh.client。
# 用户的 profile patch 后应用，可按同一行 id 覆盖配置。
# 个人路径通过 /retro config 或环境变量配置，不写入分发包。
- insert:
    - id: retro
      name: dsh-retro
      config: {}
